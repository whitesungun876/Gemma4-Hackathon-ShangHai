# CareerCraft 剧本：初级后端软件工程师
## 1. 核心元信息
    属性 定义 说明
        职业名称 初级后端软件工程师 Java/Spring Boot 技术栈
        对应岗位 互联网企业中后台服务开发工程师 (1-1年经验) 对应业务系统、微服务开发等核心岗位
        难度阶梯 ★★★★☆ 4级，侧重工程规范、问题解决与系统思维
        预计通关时长 45-60 分钟 与数据分析师平行
    核心工作流 
        需求/问题理解 → 编码/调试 → 测试验证 → 方案设计与沟通 模拟“接手-处理-交付”的完整研发流程
    核心挑战 
        代码质量、系统思维、沟通协作
## 2. 世界观与导师设定
•   所在岛屿：硅屿 (Silicon Isle) - 由流动的代码光流与服务器矩阵构成的科技岛屿，中心是巨大的“编译灯塔”。
•   团队背景：作为新人加入“星海科技”用户增长中台团队，负责核心业务系统的后端开发与维护，为全岛的居民提供数字服务。
•   导师人设：
    ◦   姓名/角色：高凌 (Ling)， Tech Lead
    ◦   风格：技术扎实，要求严格，注重代码质量和工程规范。信奉“代码是最好的文档”。对新人耐心，但评审时一针见血。
    ◦   视觉参考：男，30岁左右，极客风格，常与IDE或终端界面同时出现。
•   业务方人设 (任务发布者)：
    ◦   姓名/角色：Amy， 产品经理
    ◦   风格：业务驱动，关注功能实现和交付速度，对技术细节了解不深，需要清晰的沟通。
## 3. 前置技能要求
•   了解基础的编程概念（如变量、函数、控制流）。
•   对Web服务（请求/响应）有基本认知。
## 4. 具体任务
### 任务一：漏洞捕手
背景
你入职硅屿的星海科技——我们正在给全岛的居民做统一的身份系统，新手酒馆的登录功能刚上线就有岛民反馈问题。

## 角色对话锚点
### 1. 任务开场对话（用户接任务后自动触发）
> 【高凌（拍你肩膀）】：欢迎加入硅屿！给你个小活练手——我们给新手酒馆做的用户登录系统，有岛民反馈注册完死活登不上，Issue#123就是这个。
> 【高凌】：对了，上次那个实习生写的代码，变量名全是拼音缩写，我看了三天才破译完，你可别当「现代密码学践行者」啊。代码是最好的文档，好好干，岛民都等着进酒馆喝酒呢。

### 2. 超时提示对话（用户20分钟未提交时触发）
> 【高凌】：怎么了？卡壳了？要不要给你个提示？哦对，你看看注册和登录的时候，对用户名的处理是不是一致？别着急，我又不是催DDL的老板，慢慢来。

### 3. 任务完成反馈对话
#### ✅ 高质量完成（Bug修复干净+注释详细）
> 【高凌】：可以啊！Bug修得彻底，注释写的比我都细——你这是「人形代码注释生成器」吧？以后维护的人得谢谢你。不错，岛民现在能正常登录酒馆了！
#### ⚠️ 刚好及格（Bug修了但代码混乱）
> 【高凌】：行吧，Bug是修了...但你这代码，缩进空格混用，是要当「混元形意代码掌门」？下次注意点，代码是最好的文档啊。
#### ❌ 未修复成功（提交结果错误）
> 【高凌】：不对啊，你这改完，岛民还是登不上...再看看？是不是注册的时候没给用户名trim？别急，再试试。

具体指令与交付物
•   你的输入：
    1.  包含Bug的 UserService.java 源代码文件（见下方Mock数据）。
    2.  GitHub Issue #123 的描述：“当用户名包含空格时，用户注册成功，但后续无法登录。初步怀疑是注册和登录时对用户名的处理不一致。”
•   你的操作：
    1.  复现Bug：分析提供的代码，编写一个单元测试 UserServiceTest.java，精确复现Issue中描述的Bug场景（用户名包含空格时注册成功但登录失败）。
    2.  定位与修复：阅读代码，定位导致问题的根本原因，并修复 UserService.java 中的Bug。
    3.  验证与总结：运行你的测试，确保Bug被修复且未引入回归问题。用一句话总结Bug的根因。
•   交付物要求：
    ◦   修复后的 UserService.java 源代码文件。
    ◦   用于复现和验证Bug的 UserServiceTest.java 单元测试文件。
    ◦   一句关于Bug根因的文本总结。
Mock数据与资源
// 文件: UserService.java (有Bug的版本)
public class UserService {
    private Map<String, String> userDatabase = new HashMap<>(); // 模拟数据库， key: username, value, password
    public boolean register(String username, String password) {
        // Bug: 注册时未对用户名做trim处理，导致"alice"和"alice "被视为不同用户
        if (userDatabase.containsKey(username)) {
            return false; // 用户已存在
        }
        userDatabase.put(username, password);
        return true;
    }
    public boolean login(String username, String password) {
        // 登录时，密码验证逻辑正确，但key是原始用户名
        String storedPassword = userDatabase.get(username);
        return storedPassword != null && storedPassword.equals(password);
    }
}
// 文件: 一个不完整的 UserServiceTest.java 框架 (玩家需补充)
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
public class UserServiceTest {
    @Test
    public void testRegisterAndLoginWithSpaceInUsername() {
        UserService service = new UserService();
        // TODO: 请编写测试用例，复现并验证Bug
    }
}
技能映射
•   debugging（代码调试）
•   unit_testing（单元测试）
•   code_quality（代码质量）
评估标准 (Rubric)
评估维度 权重 评分标准 (AI评审依据)
Bug复现与测试 30% 编写的单元测试能准确、完整地复现Issue描述的Bug场景（用户名含空格导致登录失败）。
修复正确性 40% 提交的代码修复方案能彻底解决问题（如对用户名进行trim），且未引入新的逻辑错误。
代码质量 20% 修复后的代码简洁、清晰，符合基础规范（命名、缩进），无安全硬伤（如密码明文存储）。
根因总结 10% 用一句话清晰、准确地总结了Bug的根本原因（如“注册和登录时用户名比对因空格导致不一致”）。

---

### 任务二：需求炼金术师
背景
你证明了自己的能力之后，产品经理Amy找你来了——我们要给全岛做个签到功能，让岛民更愿意来星海的服务里活跃。

## 角色对话锚点
### 1. 任务开场对话
> 【Amy（晃着需求文档）】：哈喽哈喽！我们要做个签到功能！用户每天来签个到，给点奖励，就像隔壁岛的那样！需求就这一句话，你看着弄！
> 【高凌（在旁边补了一句）】：别听她的，她的需求全是模糊的。程序员的核心能力，就是把模糊的需求炼成清晰的规格。对了，上次那个实习生，没问清楚就瞎写，最后做出来的东西完全不对，可别学他。代码是最好的文档，你得把需求问清楚了再动手。

### 2. 超时提示对话
> 【Amy】：怎么样啦？岛主催我要这个功能好久了，说要给新手酒馆做引流呢...要是有啥不清楚的你随时问我啊！

### 3. 任务完成反馈对话
#### ✅ 高质量完成（需求问的全，API设计规范）
> 【高凌】：不错！需求问的很全，API设计的也规范，后续维护的人一看就懂。果然，代码是最好的文档。
> 【Amy】：太棒了！这下签到功能终于能上线了，岛民肯定喜欢！
#### ⚠️ 刚好及格（需求问了一部分，API有点糙）
> 【高凌】：行吧，功能是能做了...但你这API，有些字段没说清楚，下次记得问清楚啊，代码是最好的文档。
#### ❌ 没问清楚就做了（需求漏了很多）
> 【高凌】：不对啊，你这连连续签到中断怎么处理都没问，做出来的东西能用吗？去跟Amy再聊聊，把需求问清楚了再来。

具体指令与交付物
•   你的输入：
    1.  与AI角色“产品经理Amy”的对话窗口。
    2.  原始模糊需求描述：“用户每天可以签到一次，签到获得积分，连续签到有额外奖励。”
•   你的操作：
    1.  需求澄清：与Amy进行至少两轮对话，通过提问澄清以下关键点：
        ▪   签到规则（每天一次如何判定？基于自然日还是24小时？）
        ▪   积分规则（每次签到获得多少基础积分？）
        ▪   连续签到规则（如何定义“连续”？奖励梯度是什么？连续中断如何处理？）
        ▪   API需要什么核心功能？（如：执行签到、查询签到状态）
    2.  API设计：基于澄清后的需求，撰写一份API设计文档 api-design.md，需包含：
        ▪   接口1: POST /api/v1/check-in：请求/响应格式、鉴权、幂等性处理。
        ▪   接口2: GET /api/v1/check-in/status：返回今日是否已签到、连续签到天数、本次可获得积分。
        ▪   核心逻辑描述：如何判断连续签到、计算奖励积分的伪代码或逻辑说明。
•   交付物要求：
    ◦   API设计文档 api-design.md。
Mock数据与资源
(无预设代码文件，沟通内容为AI动态生成)
技能映射
•   requirement_analysis（需求分析）
•   api_design（API设计）
•   communication（技术沟通）
评估标准 (Rubric)
评估维度 权重 评分标准 (AI评审依据)
需求澄清完整性 40% 在与Amy的对话中，主动、有逻辑地追问了所有关键的业务模糊点（如连续定义、中断处理），并获得了明确答复。
API设计规范性 40% 设计的两个RESTful API符合常见规范，请求/响应字段定义清晰，考虑了鉴权、幂等等基础工程问题。
逻辑严谨性 20% 连续签到判定和积分计算逻辑描述清晰、无矛盾，能处理边界情况（如跨天、中断）。

---

### 任务三：守护与演进
背景
签到功能上线之后，太受欢迎了！全岛的岛民都在用，结果旧的优惠券服务扛不住了——代码又乱又慢，高凌把重构的任务交给了你。

## 角色对话锚点
### 1. 任务开场对话
> 【高凌】：坏消息，签到功能上线之后，优惠券服务直接扛不住了！之前那个实习生写的CouponService，简直是给我们留的技术负债，我可不想当「技术负债慈善家」。
> 【高凌】：你帮我把它重构了，顺便加个新的优惠券类型的支持。好的系统是演进出来的，代码是最好的文档，别给后面的人留坑。

### 2. 超时提示对话
> 【高凌】：怎么了？重构卡壳了？没事，慢慢理，把大方法拆成小的，把硬编码的东西抽出来，就好了。

### 3. 任务完成反馈对话
#### ✅ 高质量完成（重构干净，扩展方案清晰）
> 【高凌】：可以啊！重构的太干净了，扩展方案也做的很优雅——你这是「熵增对抗者」啊，在混沌的代码里强行建秩序。不错，这下服务能扛住岛民的流量了！
#### ⚠️ 刚好及格（重构了但是有点糙）
> 【高凌】：行吧，功能是能用了...但你这重构的还是有点乱，下次注意点，代码是最好的文档。
#### ❌ 重构错了（功能坏了）
> 【高凌】：不对啊，你这改完，优惠券都发不出去了...再看看？把原来的逻辑保住，再拆方法，别把功能改坏了。

具体指令与交付物
•   你的输入：
    1.  单体、结构混乱的 CouponService.java 源代码文件（见下方Mock数据）。
•   你的操作：
    1.  代码重构：分析并重构给定的 CouponService.java 类。目标包括：
        ▪   方法拆分：将超长的 issueCoupon 方法按逻辑拆分为多个小方法。
        ▪   配置抽取：将硬编码的优惠券类型和金额抽取到配置（如一个枚举类或配置文件）。
    2.  架构设计：为了支持“多种优惠券类型”（如折扣券、满减券、礼品券），设计一个简单的扩展方案。在文档中描述你的设计，例如：
        ▪   定义一个 Coupon 接口或抽象类。
        ▪   创建 DiscountCoupon, FullReductionCoupon 等实现类。
        ▪   说明 CouponService 如何根据类型创建和使用不同的券。
    3.  编写测试：为你重构后的核心逻辑编写一个单元测试，验证功能正确性。
•   交付物要求：
    ◦   重构后的 CouponService.java 及其相关配置/类文件。
    ◦   架构设计说明文档 refactor_design.md。
    ◦   核心逻辑的单元测试文件。
Mock数据与资源
// 文件: CouponService.java (待重构的版本)
public class CouponService {
    public String issueCoupon(String userId, String couponType) {
        // 一个冗长、职责不清的方法
        if (userId == null || userId.isEmpty()) {
            throw new IllegalArgumentException("User ID is required.");
        }
        // 硬编码的优惠券逻辑
        String couponCode = null;
        double discount = 0.0;
        if ("WELCOME10".equals(couponType)) {
            couponCode = "WELCOME_" + System.currentTimeMillis();
            discount = 10.0; // 10元折扣
        } else if ("DISCOUNT5PERCENT".equals(couponType)) {
            couponCode = "DISC_" + System.currentTimeMillis();
            discount = 0.05; // 5%折扣
        } else {
            throw new IllegalArgumentException("Unsupported coupon type.");
        }
        // ... 模拟数据库保存、发券等更多混杂逻辑...
        return couponCode;
    }
    // ... 其他混杂的方法
}
技能映射
•   refactoring（代码重构）
•   software_design（软件设计）
•   unit_testing（单元测试）
评估标准 (Rubric)
评估维度 权重 评分标准 (AI评审依据)
重构质量 40% 有效拆分了巨型方法，抽取了配置，使代码结构更清晰、职责更单一，可读性显著提升。
设计可扩展性 40% 提出的扩展方案（如多态设计）合理，能够优雅地支持新增优惠券类型，符合开闭原则。
测试有效性 20% 编写的单元测试能验证重构后核心发券逻辑的正确性。

---

### 任务四：线上消防员
背景
不好！凌晨的时候，监控报警了！全岛的订单接口超时了，数据库CPU直接拉满！高凌紧急拉了你，要你赶紧救火。

## 角色对话锚点
### 1. 任务开场对话
> 【高凌（紧急的语气）】：出大事了！线上订单接口挂了！数据库CPU直接干到100%，岛民都买不了东西了！
> 【高凌】：这是慢查询日志和代码片段，你赶紧看看！之前那个实习生写的代码，出问题的时候全组都得熬夜救火，可别让我们当「项目氛围组」只会喊问题不解决啊。
> 【高凌】：赶紧的，岛主都问我好几次了，你可是我们最后的希望！

### 2. 超时提示对话
> 【高凌】：怎么样了？找到问题没？要是卡壳了，你看看那个循环里的SQL...是不是N+1了？

### 3. 任务完成反馈对话
#### ✅ 高质量完成（问题找的准，优化方案到位）
> 【高凌】：可以啊！问题找的太准了，优化完之后，CPU直接降下来了！你这是「赛博华佗」啊，专治代码疑难杂症！
> 【高凌】：不错，事故报告也写的很清楚，果然，代码是最好的文档，以后我们就按这个来规范。
#### ⚠️ 刚好及格（问题修了，报告写的糙）
> 【高凌】：行吧，服务是恢复了...但你这报告写的有点糙，下次注意点，事故报告得写清楚，不然下次还犯。
#### ❌ 找错问题了（优化错了）
> 【高凌】：不对啊，你这改完，CPU还是满的...再看看？是不是那个循环里的N+1查询？还有索引的问题？

具体指令与交付物
•   你的输入：
    1.  有性能问题的 OrderDAO.java 源代码片段（包含慢SQL）。
    2.  模拟的慢查询日志（见下方）。
    3.  数据库表 orders 的简化Schema。
•   你的操作：
    1.  问题定位：分析提供的代码和日志，回答选择题：导致性能问题的最可能原因是什么？（如：A. 缺少索引 B. 循环内执行SQL C. 错误的数据类型转换）。
    2.  SQL优化：针对有问题的SQL，提供优化后的版本，并解释优化原理（如，添加了哪个索引，或如何重写了查询）。
    3.  报告撰写：撰写一份简要的事故分析报告 postmortem_report.md，需包含“问题现象”、“根因分析”、“修复方案”、“后续预防措施”。
•   交付物要求：
    ◦   选择题答案（单选）。
    ◦   优化后的SQL语句及简要解释。
    ◦   事故分析报告 postmortem_report.md。
Mock数据与资源
// 文件: OrderDAO.java 问题片段
public List<Order> getOrdersByUserId(int userId) {
    String sql = "SELECT * FROM orders WHERE user_id = " + userId + " AND status = 'ACTIVE' ORDER BY create_time DESC"; // 问题1: SQL注入风险/字符串拼接
    // 模拟执行查询...
    return orderList;
}
public List<Order> getRecentOrders(List<Integer> userIds) {
    List<Order> allOrders = new ArrayList<>();
    for (Integer userId : userIds) { // 问题2: N+1查询
        allOrders.addAll(getOrdersByUserId(userId));
    }
    return allOrders;
}
-- 慢查询日志示例
# Time: 2026-04-28T02:00:00.123456Z
# Query_time: 5.123456  Lock_time: 0.000123 Rows_sent: 10  Rows_examined: 1000000
SELECT * FROM orders WHERE user_id = 12345 AND status = 'ACTIVE' ORDER BY create_time DESC;
-- 表结构
CREATE TABLE orders (
  id INT PRIMARY KEY,
  user_id INT,
  status VARCHAR(20),
  create_time DATETIME
  -- 注意: user_id 和 status 字段上没有索引
);
技能映射
•   performance_troubleshooting（性能调优）
•   sql_optimization（SQL优化）
•   incident_response（事故响应）
评估标准 (Rubric)
评估维度 权重 评分标准 (AI评审依据)
问题定位准确性 40% 准确识别出导致性能问题的核心原因（如N+1查询、缺失索引），并选择了正确的答案。
优化方案有效性 40% 提供的SQL优化方案（如添加复合索引、使用IN查询替代循环）能有效解决问题，解释合理。
报告规范性 20% 事故报告结构完整，根因分析清晰，预防措施（如代码审查、索引规范）具有可操作性。

# 统一性确认：现在，两份职业剧本 (career_software_engineer.md 与 career_data_analyst.md) 在结构、模块、详细程度上已完全对齐，均包含：
    ◦   标准化的“核心元信息”表格
    ◦   情景化“背景”
    ◦   清晰的“具体指令与交付物”
    ◦   可直接使用的“Mock数据与资源”（代码块形式）
    ◦   结构化的“技能映射”列表
    ◦   可量化的“评估标准 (Rubric)”表格
    ◦   带人设/梗的角色对话锚点
# 交付开发：现在可以将这两份文档作为最终版技术需求，正式交付给后端、前端及AI工程师，用于：
    ◦   后端：根据Mock数据和评估标准实现AI评估接口 (evaluate_submission)。
    ◦   前端：根据交付物要求设计任务提交与反馈界面。
    ◦   AI工程师：根据评估标准(Rubric)表格进行提示词工程，实现自动评分，同时基于对话锚点生成动态交互。