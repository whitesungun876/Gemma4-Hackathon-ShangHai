"""ORM models (``orm``) and Pydantic schemas (``schemas``).

This package intentionally does NOT re-export names. Always import from the
submodule directly, e.g. ``from app.models.orm import User``.
"""
