"""Subagent implementations.

Each subagent owns exactly one product (mission spec, mission artifact,
evaluation, feynman review). Implementations live in sibling modules so
import surfaces stay narrow.
"""
